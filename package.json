{
  "name": "commit-diff-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack app that displays the diff of a given commit from any public GitHub repository.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client && npm run copy:client --workspace server",
    "start": "npm run start --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "test": "npm run test --workspace server",
    "postinstall": "echo 'Run: npm run build && npm start (serves on http://localhost:1234)'"
  },
  "license": "MIT"
}
